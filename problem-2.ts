{
    //

    const removeDuplicates = (numArray: number[]) => {
        const result = numArray.filter((num, idx) => numArray.indexOf(num) === idx);
        return result; 
    }


    console.log(removeDuplicates([1, 2, 2, 3, 4, 4, 5]));





///Solved


    //
}